services:
  - type: web
    name: xhs-opinion-monitor
    env: python
    plan: free
    buildCommand: "python3 -m py_compile server.py"
    startCommand: "HOST=0.0.0.0 python3 server.py"
    envVars:
      - key: PORT
        value: 10000
